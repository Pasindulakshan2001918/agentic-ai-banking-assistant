package com.agentic.entity;

/**
 * Spending categories for automatic transaction categorization.
 * Used to aggregate and analyze customer spending patterns.
 */
public enum SpendingCategory {
    FOOD,
    TRANSPORT,
    UTILITIES,
    SHOPPING,
    ENTERTAINMENT,
    SALARY,
    TRANSFER,
    OTHER
}
