server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agenticdb
    username: admin
    password: adminpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/agentic
